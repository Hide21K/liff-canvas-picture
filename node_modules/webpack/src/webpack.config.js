const path = require('path');

module.exports = {
  entry: './src/index.js', // エントリーポイントのファイルパス
  output: {
    filename: 'bundle.js', // 出力ファイルの名前
    path: path.resolve(__dirname, 'dist') // 出力先ディレクトリの絶対パス
  },
  module: {
    rules: [
      // ここにローダーの設定を追加
      {
        test: /\.js$/,
        exclude: /node_modules/,
        use: {
          loader: 'babel-loader',
          options: {
            presets: ['@babel/preset-env']
          }
        }
      },
      // 他のファイルタイプに対するローダーの設定も追加できます
    ]
  }
};
